---
import Layout from '../../layouts/Layout.astro';
import LocationCard from '../../components/LocationCard.astro';
import { siteConfig, serviceAreas } from '../../config';
---

<Layout 
  title="Service Areas"
  description="We provide on-site tech repair services in Morden, Wimbledon, Kingston, Sutton, Croydon, Epsom and surrounding areas within a 40-minute radius."
  canonical={`${siteConfig.url}/locations`}
>
  <div class="container mx-auto px-4 py-16">
    <div class="max-w-3xl mx-auto text-center mb-12 animate-fade-in">
      <div class="flex justify-center mb-6">
        <div class="w-32 h-1.5 bg-semantic-info rounded-full" style="background-color: var(--color-semantic-info);"></div>
      </div>
      <h1 class="text-4xl font-bold mb-4 text-text-primary">Our Service Areas</h1>
      <p class="text-xl text-text-secondary">
        We provide professional on-site tech repair services throughout South London, Surrey and surrounding areas within a 40-minute radius of Morden. 
        Fast, convenient service at your location.
      </p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-8 mb-12">
      {serviceAreas.map((area, index) => (
        <div class={`animate-slide-up delay-${(index % 3 + 1) * 100}`}>
          <LocationCard
            name={area.name}
            slug={area.slug}
          />
        </div>
      ))}
    </div>

    <div class="max-w-4xl mx-auto rounded-lg p-8 border-l-4 border-semantic-info scroll-reveal shadow-card" style="background: linear-gradient(to right, rgba(35, 60, 99, 0.15), rgba(35, 60, 99, 0.05)); border-left-color: var(--color-semantic-info);">
      <h2 class="text-2xl font-bold mb-4 text-text-primary">On-Site Service Benefits</h2>
      <ul class="space-y-3 text-text-secondary">
        <li class="flex items-start">
          <span class="text-semantic-info mr-2 text-lg font-bold" style="color: var(--color-semantic-info);">✓</span>
          <span>No need to leave your home or office - we come to you</span>
        </li>
        <li class="flex items-start">
          <span class="text-semantic-info mr-2 text-lg font-bold" style="color: var(--color-semantic-info);">✓</span>
          <span>Minimal downtime for businesses</span>
        </li>
        <li class="flex items-start">
          <span class="text-semantic-info mr-2 text-lg font-bold" style="color: var(--color-semantic-info);">✓</span>
          <span>Same professional service as in-shop repairs</span>
        </li>
        <li class="flex items-start">
          <span class="text-semantic-info mr-2 text-lg font-bold" style="color: var(--color-semantic-info);">✓</span>
          <span>Flexible scheduling to suit your needs</span>
        </li>
        <li class="flex items-start">
          <span class="text-semantic-info mr-2 text-lg font-bold" style="color: var(--color-semantic-info);">✓</span>
          <span>Ideal for multiple devices or business accounts</span>
        </li>
      </ul>
    </div>

    <div class="max-w-4xl mx-auto mt-8 rounded-lg p-6 border-l-4 border-semantic-info scroll-reveal shadow-card" style="background: linear-gradient(to right, rgba(35, 60, 99, 0.12), rgba(35, 60, 99, 0.05)); border-left-color: var(--color-semantic-info);">
      <h3 class="text-xl font-bold mb-2 text-text-primary">Coverage Area</h3>
      <p class="text-text-secondary">
        We serve Morden, Wimbledon, Kingston, Sutton, Croydon, Epsom, and surrounding areas within a 40-minute drive radius. 
        If you're unsure if we cover your area, please <a href="/contact" class="link-accent text-semantic-info">contact us</a> and we'll let you know.
      </p>
    </div>

    <div class="text-center mt-12">
      <a 
        href="/contact" 
        class="btn-primary glow-effect inline-block"
      >
        Schedule On-Site Service
      </a>
    </div>
  </div>
</Layout>

